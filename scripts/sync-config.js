export const syncConfig = {
  enabled: true,
  baseUrl: 'https://gofima.online',
  pullOnStartup: true,
  pushDebounceMs: 500,
  pullIntervalMs: 1500,
  keepAliveIntervalMs: 60 * 60 * 1000 // 1 час
};
