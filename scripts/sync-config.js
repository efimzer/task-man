export const syncConfig = {
  enabled: true,
  baseUrl: 'https://task-man-rf22.onrender.com',
  pullOnStartup: true,
  pushDebounceMs: 500,
  pullIntervalMs: 1500,
  keepAliveIntervalMs: 60 * 60 * 1000 // 1 час
};
